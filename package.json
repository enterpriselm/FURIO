{
  "name": "furio",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "setup": "python3 -m venv venv && . venv/bin/activate && pip install -r requirements.txt",
    "start:api": "venv/bin/uvicorn api.api:app --host 0.0.0.0 --port 8000 &",
    "start:contact": "venv/bin/uvicorn api.contact:app --host 0.0.0.0 --port 8001 &",
    "start": "npm run start:api && npm run start:contact",
    "stop": "pkill -f 'uvicorn'"
  },
  "author": "",
  "license": "ISC"
}
