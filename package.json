{
  "name": "FURIO",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build step required'",
    "vercel-build": "echo 'No build process for vercel'"
  },
  "dependencies": {
    "fastapi": "^0.95.0",
    "uvicorn": "^0.18.0"
  }
}
